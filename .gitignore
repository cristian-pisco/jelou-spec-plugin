node_modules/
.DS_Store
*.log

# UI QA — raw Playwright traces stay local; only summaries + screenshots are committed
.spec-workspace/**/trace/*.zip

# UI QA — auth fixtures must never be committed (per references/auth-fixtures.md)
**/.auth/
**/storageState*.json

# UI QA — Playwright run output
playwright-output/
test-results/

# Tracing — workspace-local span store, never commit raw traces
.traces/

# Docs — design/spec/plan docs are kept local, not version-controlled
docs/
