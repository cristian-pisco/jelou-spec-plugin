# Workflow: create-pr (DEPRECATED)

> This workflow was renamed to `ship`. The `/jlu-create-pr` alias executes
> `jelou/workflows/ship.md`. This file exists only to satisfy harness parity; do
> not add logic here. Use `/jlu-ship`.
