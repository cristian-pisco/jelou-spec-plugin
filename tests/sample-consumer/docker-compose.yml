services:
  api:
    build: ./api
    ports:
      - "4001:4001"
    environment:
      NODE_ENV: test
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:4001/health"]
      interval: 2s
      timeout: 1s
      retries: 15
