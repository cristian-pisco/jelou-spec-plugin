# Golden set (SEED — placeholder)

These are **synthetic seed examples**, not mined production data. They exist only so
the Stage-4 regression harness (`bin/trace-regress.mjs`) has something to run against
today. They are a placeholder until a real golden set is mined from production traces,
biased toward observed `error_signature` / `failure_mode` cases. Do **not** treat the
scores here (or in `baseline.json`) as meaningful signal yet — replace this whole
directory once real traces exist.
